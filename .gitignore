node_modules
dist
.svelte-kit
build
coverage
.env
.env.*
!.env.example
package

# Local agent tooling (Claude Code)
.claude/

# Dagger: generated SDK client + module deps (regenerated by `dagger call` / `dagger develop`)
.dagger/sdk/
.dagger/node_modules/
